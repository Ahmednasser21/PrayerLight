<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrayerLight" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor"> @color/second</item>
        <item name="android:navigationBarColor">@color/second</item>
    </style>

    <style name="Theme.PrayerLight" parent="Base.Theme.PrayerLight" />
</resources>